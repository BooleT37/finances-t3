import { Typography } from "antd";
import styled from "styled-components";

const { Title } = Typography;

export const SubscriptionCategoryName = styled(Title)`
  h3.ant-typography& {
    margin: 10px 0 0;
  }
`;
